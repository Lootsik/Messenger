#include <Engine\Engine.h>
#include "MessageManager.h"
/*void MessageManager::SendMessageUser(ID_t ID_from, ID_t ID_to, std::wstring message)
{
	Chats[SingleChat].Messages.push_back(new Message{ ID_from, false,message });
	if( _Engine.Accounts().UserOnline(ID_to))
	{
		//_Engine.SendNewEventNotification(ID_to);
	}
}
void MessageManager::GetMessageUser(Connection* connection, size_t index)
{
	//_Engine.SendMessageUser(connection, Chats[SingleChat].GetUserMessage(index)->Sender, index, Chats[SingleChat].GetUserMessage(index));
}*/